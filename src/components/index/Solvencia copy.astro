---
const solvencia = [
  {
    label: "Solicitado por el organismo de control SEPS",
    value: "9 %",
    tone: "regulatorio",
  },
  {
    label: "Cooperativa PAIS Ltda.",
    value: "12.77 %",
    tone: "cooperativa",
    destacado: true,
  },
];
---

<section class="section solvencia-compare">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <p class="eyebrow mb-10">Solvencia</p>
        <h2 class="color-brand-1 mb-15">Nivel de solvencia frente al mínimo</h2>
        <p class="subtitle">Mostramos el porcentaje exigido y el alcanzado por la Cooperativa PAIS Ltda.</p>
      </div>
    </div>

    <div class="compare-wrap">
      {solvencia.map((item) => (
        <div class={`bar-card ${item.tone} ${item.destacado ? "destacado" : ""}`}>
          <div class="bar">
            <div class="bar-fill">
              <div class="value">{item.value}</div>
            </div>
          </div>
          <p class="label">{item.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style is:inline>
.solvencia-compare {
  border-radius: 20px;
  padding: clamp(32px, 4vw, 52px);

  /* Más limpio pero con profundidad */
  background:
    radial-gradient(900px 420px at 18% 10%, rgba(0, 85, 187, 0.06) 0%, transparent 55%),
    radial-gradient(700px 360px at 92% 90%, rgba(163, 22, 41, 0.05) 0%, transparent 58%),
    linear-gradient(180deg, #ffffff 0%, #f7f8fa 100%);
  border: 1px solid rgba(15, 23, 42, 0.06);
}
  .eyebrow {
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #a31629;
  }

  .subtitle {
    color: #5e5e5e;
    max-width: 760px;
    margin: 0 auto;
  }

  .compare-wrap {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: clamp(24px, 4vw, 36px);
    margin-top: clamp(28px, 4vw, 40px);
    align-items: end;
  }

.bar-card {
  background: rgba(255, 255, 255, 0.92);
  border-radius: 16px;
  padding: clamp(18px, 3vw, 24px);
  box-shadow: 0 12px 26px rgba(15, 23, 42, 0.08);
  text-align: center;
  position: relative;
  overflow: hidden;
}

  .bar-card::after {
    content: "";
    position: absolute;
    inset: 10px;
    border-radius: 12px;
    border: 1px solid rgba(0, 0, 0, 0.03);
    pointer-events: none;
  }

.bar-card.destacado {
  box-shadow: 0 16px 34px rgba(164, 22, 41, 0.14);
  transform: translateY(-2px);
  border: 1px solid rgba(163, 22, 41, 0.10);
}

.bar {
  height: clamp(220px, 32vw, 280px);
  border-radius: 14px;
  background: linear-gradient(180deg, rgba(15, 23, 42, 0.05) 0%, rgba(15, 23, 42, 0.02) 100%);
  padding: 10px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

  .bar-fill {
    width: 72%;
    border-radius: 12px 12px 10px 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    padding-bottom: 12px;
    color: #fff;
    font-weight: 900;
    box-shadow: inset 0 10px 20px rgba(255, 255, 255, 0.22), inset 0 -10px 16px rgba(0, 0, 0, 0.1);
  }

  .bar-card.regulatorio .bar-fill {
    height: 55%;
    background: linear-gradient(180deg, #1c74b8 0%, #0e57a5 60%, #0b3f80 100%);
  }

  .bar-card.cooperativa .bar-fill {
    height: 78%;
    background: linear-gradient(180deg, #e84d6b 0%, #d33962 45%, #932c5e 100%);
  }

  .bar-card.destacado .bar-fill {
    border: 2px solid rgba(255, 255, 255, 0.32);
  }

  .value {
    font-size: clamp(1.4rem, 2.4vw, 1.9rem);
    text-shadow: 0 4px 14px rgba(0, 0, 0, 0.18);
  }

  .label {
    margin-top: clamp(14px, 2vw, 18px);
    font-weight: 800;
    color: #4a4a4a;
    line-height: 1.4;
  }

  .bar-card.regulatorio .label {
    color: #0d3b80;
  }

  .bar-card.cooperativa .label {
    color: #7d1826;
  }

  @media (max-width: 768px) {
    .solvencia-compare {
      border-radius: 16px;
    }

    .bar-card.destacado {
      transform: none;
    }
  }
</style>
